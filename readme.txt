FW and DNS Log Parser

FW script:
Outputs the source and destination IP addresses in the log file in order.
Output format:
(src OR dst)(frequency)(ip address)



DNS script:
Outputs the most frequent DNS server in order.
Output format:
(frequency)(dns server)